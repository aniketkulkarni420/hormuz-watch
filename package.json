{
  "name": "hormuz-watch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Strait of Hormuz monitoring dashboard — Cloudflare Pages (static + Functions) + KV/D1.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  }
}
